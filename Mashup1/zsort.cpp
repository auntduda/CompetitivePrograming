#include<bits/stdc++.h>

using namespace std;

#define lli long long int
#define all(x) (x).begin(), (x).end()

bool myfunction (lli a,lli b)
{
    return (a>b);
}

int main()
{
    int n, k=0;

    scanf("%d", &n);

    vector<lli> vet(n);

    for(int i=1; i<=n; i++)
    {
        scanf("%lld", &vet[i]);
    }

    sort(all(vet));

    

    for(int i=0; i<n; i++)
    {
        p
    }

    return 0;
}