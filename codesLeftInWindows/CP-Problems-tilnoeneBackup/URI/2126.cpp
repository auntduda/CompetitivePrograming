#include <stdio.h>

using namespace std;

int main(){
    char n[10000000100], dig;
    long long int i, c=0, seq=0;
    while(scanf("%s", n)){
        dig = scanf("%c", &dig);
        if(*dig=="\n"){
            continue;
        }
        c++;
    }

    return 0;
}